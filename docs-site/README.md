# Docs Framework (VitePress)

This folder contains a browsable docs site using VitePress.

## Run locally

```bash
cd docs-site
pnpm install
pnpm dev
```

Then open `http://localhost:4174`.

## Build static output

```bash
cd docs-site
pnpm build
```

Generated files will be under `docs-site/docs/.vitepress/dist`.
