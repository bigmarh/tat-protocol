export const defaultConfig = {
    relays: [
        /* "wss://relay.damus.io", */
        "ws://localhost:8080",
        /* "wss://relay.nostr.band/all", */
       /*  "wss://relay.snort.net"  */
    ]
};
